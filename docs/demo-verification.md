# 首条演示链路验证记录

验证日期：2026-09-08（Asia/Shanghai）。环境：macOS arm64、Go 1.26.1、Node.js 22.22.1。

## 已完成的验证

| 验证 | 结果 |
| --- | --- |
| `npm ci --ignore-scripts`、`npm run build` | 锁定并嵌入本地 3D 查看器 |
| `go test ./... -count=1` | 全部通过；浏览器夹具默认跳过 |
| `go test -race ./... -count=1` | 全部通过，无检测到的数据竞争 |
| `go vet ./...`、Go 可执行文件构建 | 通过 |
| `openspec validate first-asset-demo --strict` | 通过 |
| Eino + 真实 DeepSeek SDK + 本地协议响应 | 验证工具消息、thinking 配置以及澄清恢复后的 reasoning_content 历史保留；未调用真实 DeepSeek |
| Tripo 本地协议测试 | 验证生成/减面参数、任务查询、异常响应分类；未知提交只尝试一次 |
| 服务状态与边界 | 澄清恢复、已知任务恢复不重发、预算持久化、停止、3 次生产上限、3 个名额 + 10 个等待位置、FIFO 顺序、匿名归属隔离、期限清理、导出脱敏通过 |
| GLB 检查 | 合格、超面数、超体积、截断、外部资源、压缩、动画、非法索引、场景循环及过大展开数据等用例通过 |

## 受控浏览器链路

浏览器连接测试夹具服务，使用实际 Eino Runner、固定决策模型与合成 GLB。流程包含一次澄清、首次生成、实测失败、减面、重新检查及交付。

| 项目 | 实测证据 |
| --- | --- |
| 原始输入 | 给我的俯视角游戏原型做一个低模木箱。 |
| 生产提交 | 2 / 3 |
| 模型调用 | 8 / 20，均为本地受控响应 |
| 第一个候选 | 6,000 个三角面，36,656 字节，面数超过 5,000 上限 |
| 最终候选 | 12 个三角面，720 字节，三类技术检查通过 |
| 页面预览 | model-viewer 加载成功；最终自动选择第二个候选，手动切换可查看第一份失败报告 |
| 下载 | HTTP 200，`model/gltf-binary`，GLB 文件头正确，720 字节 |
| 会话 | 刷新保留当前会话；第二会话可单独创建并停止，原会话可找回 |
| 执行记录 | 38 条事件；导出 `source=controlled-test`，分开记录提议、Runtime 和真实测试工具结果 |
| 浏览器控制台 | 复测无 error 或 warning |

合成夹具故意使用重复三角面制造超限场景，最终立方体没有木箱外观。这验证的是生产闭环、实测报告和展示行为，不构成真实生成效果或真实 Agent 决策质量证据。

本次浏览器验证保留了 [受控执行记录](demo/controlled-trace.json) 和 [页面截图](demo/controlled-demo.png)。二者均为测试证据。

## 真实 DeepSeek / Tripo 链路

2026-09-08 01:06:42 至 01:12:54（Asia/Shanghai），使用用户本地配置的真实密钥运行同一条木箱需求。没有修改业务代码或改用测试供应商。

| 项目 | 实测证据 |
| --- | --- |
| Agent 模型 | DeepSeek `deepseek-v4-pro`；thinking enabled、reasoning_effort high |
| 澄清 | 0 轮；Agent 判断需求明确，公开采用默认面数与体积上限 |
| 意图与计划 | 俯视角游戏原型用低模静态木箱；生成、实测、依据报告交付或纠偏 |
| 生成目标 | 2,000 个三角面，standard 贴图质量 |
| 验收上限 | 5,000 个三角面、10,485,760 字节，始终保持不变 |
| Tripo 任务 ID | `1ce663c1-18ee-4ca3-ae91-4b1ad5f50c67` |
| 实际调用 | 5 次模型调用、1 次生产提交；首次通过，没有调用减面或重新生成 |
| 总耗时 | 371.630 秒，约 6 分 12 秒，含本地重启验证时间 |
| 最终模型 | 静态、自包含、非空三角面 GLB；2,645 个三角面、910,572 字节，三类技术检查均通过 |
| 供应商用量 | Tripo 最终任务响应报告 30 credits；没有把每次轮询中的累计值相加，也未推算货币价格 |
| 浏览器 | 预览加载成功；刷新恢复原会话；模型和记录均通过页面下载成功；最终刷新后无控制台 error 或 warning |
| 记录与单次核验 | 67 条持久事件，`source=live`；预算、交付证据及可核验违规提议检查均通过 |

在 01:11:06 重启本地服务后，页面自动重连并继续查询同一 Tripo 任务。重启前后生产次数均为 1、模型调用次数均为 4，原截止时间 `2026-09-07T17:37:02.924643Z` 未改变；导出只有一条生产提交成功记录。重启时的预期 WebSocket 断连不计为预览加载失败。

本次没有发生真实澄清或纠偏；这些分支由前面的受控测试覆盖，不能据此声称真实减面接口和多轮澄清已完成联调。实际模型的 2,645 面超过生成目标 2,000 面，但低于验收上限 5,000 面，展示了生成参数与实测验收必须分别保存的必要性。

证据文件：[真实 GLB](demo/live-crate.glb)、[技术报告](demo/live-technical-report.json)、[完整脱敏记录](demo/live-trace.json)、[运行摘要与文件 SHA-256](demo/live-run.json)、[页面截图](demo/live-demo.png)、[模型预览](demo/live-model-preview.png)。这些项目证据独立于线上会话的 7 天自动清理；没有保存访问 Cookie 或 API 密钥。

## 后续工作

依据 CONTEXT 的独立门槛建立并运行 20 个固定案例 × 3 次真实 LLM 评测，记录全部失败与各维度结果；本次首条链路没有完成此项。一次真实交付和单次规则核验不能替代 Agent 决策质量评测，也不构成视觉符合性结论。

OpenSpec `first-asset-demo` 的 11 项实现与验收任务现已完成。变更尚未归档，代码与证据尚未提交到 Git。
