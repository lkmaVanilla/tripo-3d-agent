# 初始历史输入选择修复与复验计划

第四批 84 例完整结果已保存为[未通过](2026-09-14-fourth-v3-results.md)。连续组两例错误默认可见历史版本，一例模型响应截断；三例使意图和策略为21/24，不能标记47/47。继续沿用户选择的修复与完整验收路径，不覆盖前一批结果。

## 本次修复

仅对当前 v3 模型输入增加 `input_binding`：从已持久化 `InputVersion` 派生 `not_selected/selected` 和真实版本ID，明确历史版本/报告是可用背景事实而非用户输入授权。它不另行落库，不替代同会话校验和文件前置检查，也不宣称选中即技术通过。

在原系统指令、意图和资产加工 Skill、set_intent 工具说明中明确：需要初始历史输入时先澄清选择，再保存制作意图；用户明确拒绝选择则零生产 answer。首次文本生成不需要历史输入，本Run已有候选的合法纠偏也不要求重新选择初始历史输入。

保持模型与8192输出上限、预算、工具门禁、协议纠正上限不变；不将第四批空响应归因于输入歧义，不新增掩盖空响应的重试，也不修改评测案例或阈值。

## 定向与完整复验

先按原 `missing_reference` 场景运行三次独立诊断，原始记录来源仍为第三批同场景；三次自动与全提议语义均通过、关键错误为零才进入下一批完整84例。实际场景完全一致，已明确拒绝选择后提前answer合法，不机械要求三轮。

随后在新的源码冻结清单和独立目录运行完整 current-v3 20×3+8×3，真实DeepSeek加固定Provider、零真实Tripo。维持每Run20模型调用/3生产，完整批次最多1680模型调用。每组每个适用维度至少90%、关键错误零，审核包括全部原始提议、模型失败和Runtime拦截；不选择性补分。

本次只对最终代码重新生成验收证据，不改写任何历史批次。使用新目录记录系统回归，不覆盖先前系统通过日志；浏览器、真实Tripo、升级详细演练在对应功能未改变时沿用已有证据。仍不提交、归档、部署或替换原服务和数据。未满足门槛的任务保持未完成。
