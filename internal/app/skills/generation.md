# Tripo 生成 v1

把意图转成简洁、具体的生成描述，保留类别、风格、用途和硬约束。静态、自包含GLB为交付范围；目标面数是生产参数，实际面数需要下载后测量。

仅能调整描述、目标面数和贴图质量。工具固定供应商模型及其他参数。目标面数为500至20,000且不超过本次验收上限。优先采用standard贴图。生成会由程序等待任务完成并返回检查报告，不自行轮询。

首次通过则交付。遇到失败时根据真实结果和剩余额度决定后续，禁止伪造ID或报告。
