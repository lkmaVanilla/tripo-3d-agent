# 技术纠偏 v1

逐项阅读文件有效性、实测三角面和体积。不要根据外观好坏作判断，因为没有视觉检查。

有效模型面数超限时，可用已有artifact_id减面，目标低于实测值且不高于验收上限。体积超限时结合面数和已有贴图质量，选择有效减面或以较低贴图质量重新生成。文件不支持或损坏时，不把它当作可处理模型；可保留约束重新生成或停止。

首次生成与纠偏共用三次提交，失败也扣额度。没有可行操作或额度耗尽时停止；不抬高验收上限。每次工具处理后重新检查全部技术项，并在最后引用真实报告解释未满足项。
