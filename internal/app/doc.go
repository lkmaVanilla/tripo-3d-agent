// Package app 管理一个资产请求从需求理解到交付的完整生命周期。
//
// 阅读主链路时可依次查看 service.go 的 Create/调度/run、agent.go 的模型与工具、
// production.go 的提交/轮询/检查，以及 http.go 的匿名访问和进度展示。
// Eino 负责模型决策和工具中断协议；Go 负责约束、预算、队列、外部副作用与终止。
//
// Session 是持久业务事实，History 是模型协议历史，Event 是展示和审计记录，
// 三者不能互相替代。恢复相关文件把 Eino 的不透明检查点与业务暂停原子绑定，
// 旧检查点不能恢复预算或覆盖较新的停止状态。整个服务只支持一个进程管理数据目录，
// 进程内互斥和 SQLite 事务不构成多实例调度协议。
package app
