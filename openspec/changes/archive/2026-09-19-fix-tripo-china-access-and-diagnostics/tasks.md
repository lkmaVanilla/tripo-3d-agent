## 1. 国内站配置与只读接入预检

关联：[tripo-provider-access](specs/tripo-provider-access/spec.md) 的基址、模型协议及预检要求；[设计决策 1–2](design.md)。以下均为待实施任务。

- [x] 1.1 增加 `TRIPO_BASE_URL` 配置、国内缺省与官方 V3 地址校验，贯通应用配置和客户端构造；配置测试验证缺省、显式覆盖、尾部斜杠及非法地址拒绝，失败时不泄露配置凭证或回退站点。
- [x] 1.2 让提交、查询、上传共用有效基址，保持 H3.1 生成、`v2.0` 减面与独立下载策略；协议测试验证各阶段目标地址、模型标识、下载不带 API Key，以及未设置的验收上限不被补回。
- [x] 1.3 增加复用同一配置的最小只读预检入口和 `.env.example` 说明；受控测试验证正常响应、401、供应商拒绝、超时／解析失败的退出状态，并断言只请求余额接口、不启动 Run 或上传／生产。

## 2. 结构化错误与逐次尝试记录

关联：[provider-diagnostics](specs/provider-diagnostics/spec.md) 的阶段证据、逐次尝试与脱敏要求。

- [x] 2.1 扩充类型化错误及安全错误说明，保留底层 cause 用于分类而非输出；测试覆盖 DNS、连接、TLS、超时、取消、HTTP／供应商错误、非 JSON 和缺字段响应，验证耗时、可选字段、追踪标识及 Unknown 语义不丢失。
- [x] 2.2 贯通上传、本地输入校验、下载读取及远端任务失败的诊断；测试验证查询成功而任务失败使用独立任务错误码，下载／上传失败不冒充提交失败，未发起请求的本地错误不伪造 HTTP 状态。
- [x] 2.3 实现诊断白名单、长度限制和一致的安全投影；恶意输入测试覆盖句中签名 URL、Authorization、API Key、上传 token、控制字符、超长正文和嵌套字段，断言事件存储及服务器日志不包含敏感哨兵值。
- [x] 2.4 在 Runtime 各次调用边界写入诊断事件与关联日志，包含尝试身份、重试组和组内序号，避免收尾重复记录；测试验证查询前两次失败后成功、上传／下载重试耗尽及重启后新尝试均可追踪，原重试上限和截止时间不扩大。

## 3. 保留未知提交与 checkpoint 一致性

关联：[tripo-provider-access](specs/tripo-provider-access/spec.md) 的不隐式重发要求、[provider-diagnostics](specs/provider-diagnostics/spec.md) 的记录一致性要求，以及现行 `checkpoint-recovery`。

- [x] 3.1 禁止生产提交的透明重试与带凭证 API 的自动重定向，保留独立下载重定向策略；协议测试覆盖 301／302／303／307／308、连接复用失败和无可重放提交体，统计接收端真实请求，断言重定向目标没有收到请求或凭证，生产不会隐式重发。
- [x] 3.2 调整错误传递和诊断保存位置，保证提交意图／扣额先保存、TaskID 优先保存；故障注入验证提交意图保存失败不发请求，TaskID 保存失败不重提，附加诊断失败不撤销任务身份、未知状态或额度。
- [x] 3.3 保留 Runtime 对无 TaskID 未知提交、已知任务、明确供应商拒绝和迟到 TaskID 的原有分流；针对性测试验证细分 DNS／TLS／超时不授权重提，明确拒绝不扩大有限纠偏预算，用户停止后的迟到任务不改终态。
- [x] 3.4 扩展并运行已有恢复集成测试，覆盖未知提交诊断失败后重启、工具重入、强制进程退出、已知任务继续查询及输入准备恢复；断言未知操作始终只有一次生产提交、预算与原输入身份保持不变，已知任务沿用同一 TaskID。

## 4. 用户错误解释、工作台及导出一致性

关联：[chat-asset-workspace](specs/chat-asset-workspace/spec.md) 的完整修改要求，以及 [provider-diagnostics](specs/provider-diagnostics/spec.md) 的解释、归属与兼容要求。

- [x] 4.1 由 Runtime 终态和已保存诊断生成简明错误解释；结果测试区分提交未知、远端任务失败、下载失败、技术检查失败、停止和临时失败后成功，不宣称远端取消、退款或未经验证的根因，缺诊断时兼容旧记录。
- [x] 4.2 将安全诊断一致投影至 HTTP、WebSocket 和执行／会话导出；接口测试验证相同执行得到相同字段、刷新重连不重复记录、其他匿名身份被拒绝，并确认所有出口不泄露凭证、上传材料或签名 URL。
- [x] 4.3 调整现有制作卡片、错误说明及展开证据区域；网页行为测试验证未知提交说明、最终成功不被历史失败覆盖、切换会话与重连归属正确，不新增生产重试按钮，不改首页和正常产品展示文案。

## 5. 集成回归与发布文档

关联：全部规格与 [设计中的迁移计划、验证矩阵及回退边界](design.md)。这组检查覆盖跨模块行为，不替代上面各任务的局部验证。

- [x] 5.1 执行 Go 全量测试、race 与 vet，以及现有网页行为测试和构建；记录命令与结果，确认可选约束、连续会话、版本登记、checkpoint 和匿名隔离无回归；若修改证据汇总解析，则同时通过对应脚本测试，不启动完整真实模型案例集。
- [x] 5.2 编写发布操作说明，包含服务有效配置预检、实际数据目录核实、停写备份与完整性检查、一次性清空、启动核对、验收和回退边界；逐项核对覆盖“预检失败不清理”“正常重启不删除数据”和“国内新任务后不覆盖回旧国际数据库”。
- [x] 5.3 更新运行配置说明和必要的测试／证据说明，标明 V3 API、H3.1、减面算法 `v2.0` 的区别以及新诊断字段含义；检查文档与实现一致，不升级 Skill／执行协议、不恢复强制资产默认上限，也不擅自归档其他变更。

## 6. ECS 切换与真实链路验收

关联：[tripo-provider-access](specs/tripo-provider-access/spec.md) 的预检及一次性切换要求、[design.md](design.md) 的真实验收矩阵。依赖第 1–5 组完成；下列任务不能仅凭本地代码测试勾选。此组在后续实施阶段执行，本提案阶段不操作线上。

- [x] 6.1 构建可识别 revision 的候选版本，在 ECS 复核实际服务、配置、数据目录和活动任务；以目标服务有效国内配置运行只读余额预检并保存脱敏证据，仅在网络和鉴权通过、确认具备验收额度后进入切换，失败则记录问题并保留旧数据。
- [x] 6.2 暂停入口并停服，备份并验证实际运行数据；按用户已授权范围将旧会话、执行、checkpoint、资产记录与模型文件移出活动数据区，验证新目录为空且配置、仓库证据和其他项目完整，记录备份位置但不提交备份到 Git。
- [x] 6.3 部署候选版本并显式设置国内站，启动后核对二进制 revision、基址及凭证加载一致性，再次通过只读预检；确认没有恢复／查询旧国际任务，并验证后续普通重启不会重复清空国内数据。
- [x] 6.4 通过真实网页聊天及真实 DeepSeek 完成一次国内 Tripo 静态资产生成；保存脱敏 Run／Operation／TaskID、版本身份、技术检查及正式结果，确认自包含 GLB 可下载且网页加载成功，不能仅以提交或查询 HTTP 成功勾选。
- [x] 6.5 在同一会话显式引用上一步 v1，以合法且低于实测输入的目标完成一个真实减面目标；允许 Agent 在既有预算内依据已保存的不达标技术报告自动纠偏，逐次记录实际生产提交。验证上传、任务查询与下载证据完整，所有版本及未达标报告保留，首次加工输入对应 v1，后续候选的 parent 与实际输入一致，所有版本均可加载和下载，最终交付实测面数下降且满足已保存的验收上限。
- [x] 6.6 对上述会话验证刷新／WebSocket 重连／导出一致且不产生新生产请求；汇总本次代码回归、国内接入、真实生成与减面的独立结果及剩余问题，证据脱敏且不包含模型大文件，不宣称完整 Agent 案例集已重新通过。遇未知提交立即停止，不为凑齐验收结果自动重提。

## 实施记录（2026-09-19）

代码、国内 ECS 切换、真实生成和一致性核对已完成，见 [验证记录](../../../../docs/verification/tripo-china-access/verification.md)。当前 23/23。任务 6.5 的第一次减面为 1839 面，超过设定的 1500 上限；Agent 在原预算内自动纠偏后以 1473 面交付 v3。用户于 2026-09-19 回复“接受”，确认按实际包含自动纠偏的链路验收，任务 6.5 据此完成；v2 未通过的原报告继续保留。实际为一次生成、两次减面，共 3 次生产提交，没有人工新开任务重跑，也没有未知提交重试。本次确认仅更新验收文档和任务登记，未新增模型或供应商调用，没有修改或归档其他变更。
