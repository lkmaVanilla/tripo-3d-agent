## 1. 对齐受众并固定会话执行版本

- [x] 1.1 冻结基线 v1 的系统指令、Agent/工具描述与参数 schema、Skill 目录和正文，建立不引用最新配置的旧版夹具；验证冻结内容与 `49a1364` 一致，并可由真实 Eino 构造旧暂停与旧工具调用。（`agent-audience`：提示升级保留在途会话的执行版本）
- [x] 1.2 编写 v2 非网页受众指引，仅修改系统身份、需求 Skill 及相关描述中的冲突表达，并补充正式结果与原始解释的职责；检查新配置面向实际创作用途、仍保留静态 MVP/Tripo-only/原预算，记录本次涉及和未涉及的 Skill。（`agent-audience`：按实际用途理解创作者需求、受众扩大不扩大当前生产权限）
- [x] 1.3 增加会话配置版本解析，将所有 Runner 入口、Skill 加载、模型调用事件和 ReplaySeed 创建/校验绑定到该版本，覆盖无 Seed 的普通 Resume 与 legacyPending；用测试确认新会话为 v2、旧缺字段会话为 v1、矛盾或未知版本拒绝，Skill 哈希与实际内容一致且不放宽输入校验。（`agent-audience`：提示升级保留在途会话的执行版本）

## 2. 根据证据生成正式结果

- [x] 2.1 实现小型版本化终态依据与统一事实构造/格式化函数，重用已存报告和预算；测试覆盖真实面数与生成目标不同、模型伪造绑定/动画、检查失败与面数无法验证，证明自由解释不影响正式事实。（`verified-results`：正式结果以实测证据为依据）
- [x] 2.2 接入 `finish_request`，保留原三个输入字段、交付校验和原始提议记录；测试合格交付、不存在/跨会话/未通过的引用、尚有额度时 Agent 结束及结束说明声称成功，确认正式状态与事实不受自由文字改写。（`verified-results`：Agent 决策与事实核验分离）
- [x] 2.3 将用户停止、空闲/执行到期、错误与预算收尾、已知任务确定性恢复接入共同结果构造，并与终态和结束事件同事务保存；通过各入口测试核对未知提交、无合格产物、有合格产物兜底及恢复收尾，确认不增加模型调用、生产次数或延长期限。（`verified-results`：各终态准确表达已知范围、结果与终态一致提交）

## 3. 保持输出兼容与审计边界

- [x] 3.1 让公开视图、会话接口、WebSocket 和导出复用正式结果，保留 `session.final` 字符串和状态枚举；以同一终态的接口对比测试确认结论、报告引用、版本和来源一致，现有前端无需修改即可消费。（`verified-results`：输出接口共享正式结果并保持网页不变）
- [x] 3.2 保留带来源的原始模型说明/外部错误证据，并核验正式结果与状态/报告的一致性；测试原始错误说明仍可追踪、结构化违规提议不被抹除、自由解释语义未验证时标为无法验证，以及匿名访问和脱敏规则不回退。（`verified-results`：正式结果核验不代替 Agent 评测）
- [x] 3.3 实现仅限旧终态的只读结果与候选报告投影：有证据时重建，历史 completed 缺证据时公开为已结束，无据报告不透传通过标记，原始状态/说明/报告保留审计来源；测试数据库原记录、事件、Ended/Expires 不变，默认最新候选也不误报通过，其他有完整证据的候选不被误降级，在途 runtime_state 不受影响。（`verified-results`：历史说明不得冒充已验证事实）

## 4. 验证升级与收尾的一致性

- [x] 4.1 使用冻结 v1 配置与真实 Eino 生成已提交问题、已接受答案、排队及尚无 checkpoint 的待提交草案，在默认 v2 服务中恢复；验证问题/WaitID/操作参数与版本不变、输入哈希匹配、纯重建不新增模型调用或队列效果。（`agent-audience`：提示升级保留在途会话的执行版本；现行 `checkpoint-recovery`）
- [x] 4.2 覆盖旧已知任务续查、旧未知提交不重发、恢复后原 finish JSON 参数仍可结束的升级路径；验证原 TaskID、额度、期限与旧工具参数保持，v1 的自由解释也不进入正式事实。（`verified-results`：各终态准确表达已知范围；现行 `checkpoint-recovery`）
- [x] 4.3 补充未知/矛盾版本、损坏种子、停止后的迟到结果、结果事务失败及重复收尾回归，复用现有故障夹具；确认拒绝行为、终态优先和事务一致性，不能通过重算摘要、清空检查点或覆盖历史来通过测试。（`verified-results`：结果与终态一致提交；`agent-audience`：恢复版本不可证明）

## 5. 验证与记录

- [x] 5.1 执行受影响专项、全量 `go test -race ./... -count=1` 与 `go vet ./...`，保留结果；确认现有并发、隔离、强制退出恢复和预算回归通过，受控运行未调用真实 Tripo。
- [x] 5.2 按 design 决策 7 的四个定向案例各执行一次真实 LLM 加固定 Tripo 响应的检查，保留全部输入、提议、Skill 版本、结果与逐项判断；验证非游戏用途、游戏用途、未接入能力边界和超限解释。如凭证或模型不可用，记录原因并保持本项未完成，不以受控模型代替。
- [x] 5.3 在 `docs/` 添加本变更验证记录，分别报告受控系统回归与真实 LLM 小样本，记录旧配置兼容、历史输出投影、上线/回退边界与剩余限制；核对完整 60 次 Agent 评测状态未被改成通过，`git diff 49a1364 -- internal/web/` 无差异。
- [x] 5.4 运行 `openspec validate align-agent-audience-and-ground-results --strict` 与全项目严格校验，核对任务与证据逐项对应；只有实际完成并验证的任务才勾选，不将本提案的规划完成计作业务实现完成。
