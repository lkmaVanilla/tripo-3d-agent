## 1. 会话与单次执行的存储基础

- [x] 1.1 按 design D1 为 `internal/app` 增加 Conversation、Run 绑定、公开消息和 AssetVersion 的类型、表及唯一约束，保留原 Session 的执行语义；验证：存储测试证明一个 Run 只属于一个会话、消息提交身份和来源操作不会重复登记，原执行数据仍可读取。对应 `continuous-asset-conversation`、`session-asset-versions`。
- [x] 1.2 增加可重复执行的旧 Session 包装迁移，为旧记录建立会话及首 Run 关联，保留 ID、checkpoint、History、操作、预算、结果、文件路径和原 Ended/Expires；验证：未结束、已结束及已过期夹具连续迁移两次后映射唯一，原字段及恢复字节不变。对应 `checkpoint-recovery`。
- [x] 1.3 实现会话归属、有效期和活动 Run 的统一持久化查询，供新入口及兼容入口共享；验证：同访客多会话、其他访客、已过期会话和仍有活动执行的读取结果符合隔离与保护规则。
- [x] 1.4 将新表及后续消息、版本回填纳入有迁移版本标记的启动流程，完整迁移提交前不接纳请求或恢复执行；验证：缺失绑定、迁移事务失败和重复启动不会开放半迁移数据，也不创建替代 Run 或触发生产。

## 2. 消息接受与执行生命周期

- [x] 2.1 实现首条消息原子创建会话与 Run，以及空闲会话接受新消息建立新 Run；分别使用访客范围创建幂等键和会话范围消息幂等键；验证：重复相同载荷返回原身份，不同载荷复用键被拒绝，响应丢失重试不增加模型调用或执行。对应 `continuous-asset-conversation`。
- [x] 2.2 在事务内约束同会话单活动执行，并保留现行跨会话调度容量；验证：两个客户端并发发送最多接受一个，理解、排队、queue_full、生产与退出处理中均拒绝新目标且不扣额度，不同会话仍能并发推进。
- [x] 2.3 将聊天答案绑定到 Run、WaitID 和暂停代次，复用原答案持久化与重放机制；验证：当前答案继续原 Run、旧问题及跨 Run 答案被拒绝，重复回答不增加澄清轮次；选模答案中的版本引用经过同会话校验后固定到当前输入。
- [x] 2.4 实现停止指定 Run、持久化停止、取消 worker、退出后释放匹配活动指针及发布空闲事件；验证：停止已接受但 worker 未退出时不能发新目标，迟到结果不复活旧 Run、不覆盖下一 Run，也不声称远端已取消。
- [x] 2.5 将 20 次模型调用、3 次生产、3 轮澄清、首次生产后 30 分钟及生产前 24 小时空闲限制保持为逐 Run 规则；验证：新用户目标获得独立额度，原目标纠偏与 queue_full 重试不刷新额度，失败与未知提交仍计数，重启不改变原截止时间。

## 3. 同会话版本与真实输入

- [x] 3.1 实现 AssetVersion 的追加登记、会话内序号、文件摘要及来源关系，区分实际几何父版本与仅用于文本再生成的上下文引用；验证：v1→v2 后再次从 v1 生成加工版本 v3 时，v3 的父版本仍为 v1；文本再生成不伪造几何父关系。对应 `session-asset-versions`。
- [x] 3.2 将 `production.go` 的文件原子写入与 Artifact、版本、报告及事件的共同登记衔接，按来源操作去重；验证：重复完成和保存重试只出现一个版本，无文件失败无版本，有文件但检查失败保留候选且不自动交付。
- [x] 3.3 增加只读 InputVersion 和按本次目标生成的 InputAssessment，锁定消息引用与首个加工输入，允许纠偏使用本 Run 新产物；验证：同访客跨会话、多选、不存在、缺失或损坏文件均被拒绝，预览改变不影响输入，未达旧面数目标但文件有效的候选可减面，旧 Artifact 不被复制成新输出。
- [x] 3.4 迁移已有 Artifact 为版本记录并保留原访问映射，只按可验证操作证据回填父关系，否则标记历史未知；验证：重复迁移不复制文件、改变摘要或重置保留期，旧候选全部可追踪，不按数组相邻关系虚构加工链。

## 4. Tripo 历史模型输入准备

- [x] 4.1 在 `internal/tripo` 增加本应用已保存 GLB 的内部文件上传适配及文件 token 减面输入，核对格式、大小与输入身份；验证：受控 HTTP 服务检查 multipart 内容、文件摘要和减面载荷，覆盖超限、错误响应及超时，公开入口不接受任意文件路径、URL、token 或 TaskID。对应 `session-asset-versions`。
- [x] 4.2 持久化每个操作的 PreparedInput、语义版本身份及上传尝试计数，发送前计数、跨重启累计最多三次；验证：token 取得和重新准备始终使用同一 version/hash，不消耗生产次数、不登记输出，重复重启不能重获上传额度。
- [x] 4.3 将输入准备接入生产前置流程，进入 submitting 前冻结实际提交参数、扣额及期限；验证：生产 POST 前可以在剩余额度内重新准备，已进入提交且 TaskID 未知后绝不刷新载荷重发，已知 TaskID 只续查，停止后迟到上传不触发生产。对应 `checkpoint-recovery`。
- [x] 4.4 增加输入准备失败的结构化结果和恢复返回值，不以重新生成替代失败加工；验证：旧供应商 URL 失效时使用保存文件完成受控加工，上传失败保留旧版并准确结束，减面产物可再次上传作为下一操作输入。

## 5. Agent 上下文、Skill 与正式结局

- [x] 5.1 增加 `asset-agent-v3` profile 及独立运行状态投影，冻结 v1/v2 的提示、Skill、工具 schema/description、参数语义和恢复输入；验证：v1、v2 的 waiting、accepted_answer、pending_question、queued、pending_production 夹具恢复时原协议及 ReplaySeed.InputHash 均不变。对应 `agent-audience`、`checkpoint-recovery`。
- [x] 5.2 实现 Go 确定性上下文组装：当前原文、会话主题、明确版本及来源意图/报告、最近最多 20 条公开消息和 5 次结束摘要，并设自由正文长度上限及裁剪记录；验证：窗口外被明确引用的旧版本事实仍完整，新约束覆盖本次继承要求，硬约束及身份不丢失、不混入其他会话。
- [x] 5.3 为 v3 编写需求理解、生成、纠偏和资产加工 Skill，区分解释、无需加工、生成、减面与不支持请求；验证：受控 Agent 案例覆盖非游戏用途、明确旧版本、缺失引用、三轮后输入仍不明确及独立资产换题，未确定合法输入时零生产结束，未接入能力不被静态生成替代。
- [x] 5.4 调整 v3 工具门禁以接受锁定历史输入及本 Run 新候选，保留本 Run 已有合格输出时不继续生产的规则；验证：新目标使用新上限、旧报告不变，不能切换另一历史版本；减面失败不得隐式重生成，模糊“继续/再试”不得自动重提旧未知或已停止任务。
- [x] 5.5 增加版本化 `answer/delivery/ended` 结局与逐 Run 正式结果生成，公开事实来自保存证据，原始建议保留 Agent 来源；验证：纯解释和无需加工为零生产、零新版本，旧输入不能被交付为新产物，发生生产不能伪装为纯回答，假面数或未执行的绑定结论不进入核验事实卡，原始 Agent 文字与程序事实隔离。对应 `verified-results`。

## 6. 持久聊天与事件投影

- [x] 6.1 从已接受业务事实构建 user、clarification、accepted_plan、operation_card、answer 和 run_result 消息，并按可信来源回填旧对话；验证：刷新可重建多 Run 对话，不发布未提交问题，不公开 Eino History、内部推理或未经核验的原始提议为正式事实，不编造缺失历史。对应 `chat-asset-workspace`。
- [x] 6.2 将消息、版本及正式结局与对应公开事件在同一事务内保存，为计划、操作卡及结果设置稳定去重身份；验证：重复收尾和事务失败不产生孤立消息或重复卡片，轮询只更新同一操作卡，旧 Run 事件不更新新 Run。
- [x] 6.3 保留全局单调 `events.seq`，按会话关联跨 Run 续读，并实现一致快照水位及分页；验证：带上一 Run 游标重连可以补齐后续事件，序号间隔不被当成丢失，分页/快照/推送重叠后消息和版本均不重复。
- [x] 6.4 对终态 Run 按结果与事件水位缓存核验，当前执行增量投影；验证：受控长会话中推进一次当前操作时不反复加载全部历史或重验全部终态，缓存失效后 HTTP、WebSocket 和导出仍得到相同事实。

## 7. 会话接口与兼容入口

- [x] 7.1 实现 design D6 的会话创建/列表/快照、消息/答案、逐 Run 停止及 queue_full 重试接口，统一错误与幂等返回；验证：契约测试覆盖输入长度、忙碌、键冲突、问题代次、仅原操作重试及新接口使用 v3。对应 `continuous-asset-conversation`、`chat-asset-workspace`。
- [x] 7.2 实现分页历史、版本列表、受保护文件、会话事件订阅和逐 Run 记录导出；验证：消息默认 50 条且最多 100 条，文件/版本归属匹配，导出保留逐次预算和结果、不泄露本地路径、凭证或供应商访问材料。
- [x] 7.3 保留 `/api/sessions` 的原单 Run 请求与结果语义，旧创建入口固定 v2 并创建包装会话，旧链接解析到新工作台；验证：旧客户端读取原 Run 而非会话最新结果，旧会话继续的新消息另建 v3，续期后旧下载入口按会话期限可用。
- [x] 7.4 对所有新旧读取、命令、文件和 WebSocket 统一匿名归属、有效期及同源校验；验证：其他访客无法通过持有 ID 访问，同访客不能跨会话引用，凭证失效/会话到期后已有连接也停止暴露数据，缺失 Cookie 不凭链接恢复身份。

## 8. 聊天首页与资产工作台

- [x] 8.1 在现有 Go embed 与原生 ES modules 基础上实现首次输入、会话列表和工作台路由，拆分连接、聊天及版本视图；验证：首页接受首条消息后立即进入对应工作台，失败保留原输入与提交身份，旧链接可打开，窄屏可切换聊天和预览区域。对应 `chat-asset-workspace`。
- [x] 8.2 实现持续输入框的新目标、当前问题答案、忙碌草稿和停止处理中状态，分别保存回答草稿与后续目标草稿；验证：当前页面切换会话后草稿仍在，结束后不自动发送，停止命令返回但尚未空闲时不能发送，澄清和 queue_full 重试使用原执行身份。
- [x] 8.3 实现模型预览、可折叠版本列表、显式单版本引用及逐版本下载/报告/来源展示；验证：输入引用与预览分开，用户手动预览旧版时新结果不会强行切换，无效文件明确不可处理，不出现外部导入或未接入能力入口。
- [x] 8.4 实现持久聊天、操作进度卡、正式结果、来源标记及执行证据展开，并连接断线补读和会话到期展示；验证：切换会话后的旧连接不覆盖当前页面，远端成功与技术通过分开，纯回答无新交付卡，多次执行结果均可独立查看及导出。

## 9. 整体保留、续期与清理

- [x] 9.1 将会话到期设为最后 Run 持久化结束时间加 7 天，正式接受下一 Run 时保护全会话并在结束后重算，活动或退出中执行不清理；验证：可控时钟覆盖完成、回答、失败、停止和超时，浏览/下载/心跳/草稿不续期，生产前空闲期限仍结束执行。对应 `continuous-asset-conversation`。
- [x] 9.2 将清理所有权从独立 Session 目录改为 Conversation，停用旧独立过期删除路径；验证：后续执行跨过首 Run 原到期时间时所有历史文件仍在，原 Run.Ended/Expires 不被改写，单纯迁移沿用原会话到期时间。
- [x] 9.3 将接受新消息、过期判定和清理标记事务互斥，先阻断全部访问再幂等删除该会话关联文件及记录；验证：到期竞争只会完整接受并保护或明确拒绝，删除失败可重试，半清理状态不能经任何新旧入口访问，也不会误删另一会话。

## 10. 综合恢复与端到端验收

- [x] 10.1 扩充已有父子进程强杀测试，在包装会话、回填消息/版本以及文件落盘/版本提交边界强制退出并连续重启；验证：迁移原子且幂等，原 checkpoint 字节、历史预算/结果/时间及文件摘要保持一致，同一操作最多一个版本，没有额外远端提交。对应 `checkpoint-recovery`。
- [x] 10.2 覆盖 Run 已停止但活动指针未释放、空闲事件提交失败及后续 Run 已开始时的迟到释放；验证：重启识别旧 worker 已不存在并恢复发送，不永久停在退出中，不释放新 Run，不恢复或取回旧已停止任务。
- [x] 10.3 对上传响应前、token 保存后生产前、submitting 但 TaskID 未知、已知 TaskID 四个窗口实施受控强杀；验证：上传次数与文件身份跨重启固定、期限不刷新、未知提交零重发、已知任务续查，停机后结果和扣额与原操作一致。
- [x] 10.4 对清理标记后、部分目录删除后实施强杀，并与续期和兼容下载交叉验证；验证：重启完成幂等清理，过期数据全入口不可访问，未到期及被新 Run 保护的历史模型不被删除，Cookie 续期不改变会话期限。
- [x] 10.5 使用受控 Provider 完成“生成 v1→引用 v1 减面为 v2→再引用 v1 为 v3”的网页链路及同 Run 纠偏链，覆盖纯解释、双标签提交、刷新、断线与会话切换；验证：文件具有真实不同技术数据，三个目标独立记录、父关系正确，旧文件/报告保留，网页与导出一致。
- [x] 10.6 按 `technical-only-v1` 评测 8 类、每类 3 次的真实 LLM＋固定 Provider 连续创作，另评测既定 20 场景×3 次基线；按用户 2026-09-14 的范围调整删除全部视觉相关评分与专用场景，保留技术事实和执行决策覆盖。本次依用户追加授权，复用运行代码与 Skill 未变的完整第五批真实执行轨迹，另建 manifest 和语义审核重新核对全部 84 例、344 条提议，没有新增调用。验证：两组非视觉关键错误均为零，各适用维度均达到 90%，六项非关键失败保留；同一评分版本、完整原始证据和[新报告](../../../../docs/evaluations/2026-09-14-technical-only-reassessment-results.md)独立保存，旧成绩不覆盖。
- [x] 10.7 单独完成真实 Tripo 验证：生成文件上传后减面、减面输出再次上传加工，以及显式回到历史版本的加工；验证：保存真实 TaskID、输入摘要、父关系、文件技术报告和实际额度/费用证据，区分接口成功与目标通过，失败保留真实原因，不以局部链路替代 Agent 评测。

## 11. 文档、回归与发布交付

- [x] 11.1 更新 PROJECT_BRIEF、CONTEXT、OpenSpec 配置背景与阶段验证文档，新增会话/执行/版本及整体保留策略的 ADR，说明旧 ADR 的适用范围；验证：所有当前阶段描述与本变更的 6 项能力一致，MVP 与长期方向分开，历史验证记录保留，未接入图片/绑定/动画不被宣称可用，不归档 `first-asset-demo`。
- [x] 11.2 运行完整 Go 测试、相关包 race 回归、构建及 OpenSpec 严格校验，汇总浏览器、强杀、真实供应商和 Agent 评测证据；验证：发布记录逐项列出通过/失败/未执行，失败处理完毕才标记验收完成，不能用规格校验替代应用验证。完成依据：最新完整系统回归 8/8、评分范围调整后的 Go 9 项与 Python 23 项、既有浏览器/强杀/真实供应商/升级演练证据，以及本次完整非视觉审核。Agent 六项非关键失败按原有 90% 门槛保留并记录，未宣称修复或全例零错误；系统测试无遗留失败，详见[阶段验证](../../../../docs/verification/conversation-workspace.md)。
- [x] 11.3 交付停写备份、单进程升级、迁移后核对和回退条件的发布说明，并在独立数据副本演练；验证：SQLite 与模型目录可一起恢复，旧 checkpoint/下载可用；一旦接纳新消息或发生外部动作只以前滚修复处理，禁止用旧快照抹去新事实。

当前验收状态：用户于 2026-09-14 明确删除视觉相关评测，并追加授权按新规则重新审核。第五批全部 84 例按 `technical-only-v1` 独立审核通过：基线解释 55/58、连续创作解释 22/24，两组各维度均达到 90%，关键错误均为 0；6 项非关键失败仍保留。`10.6` 与 `11.2` 已完成，进度 **47/47**。本次没有新增模型或 Tripo 调用，旧评分原样保留；[新审核报告](../../../../docs/evaluations/2026-09-14-technical-only-reassessment-results.md)与[阶段验证](../../../../docs/verification/conversation-workspace.md)共同记录验收依据。当前变更已于 2026-09-14 同步全部 6 个能力规格并归档，尚未部署。

历史第三批：基线解释 48/57（84.21%）、关键错误 2，原[复测报告](../../../../docs/evaluations/2026-09-current-v3-revised-evaluation-results.md)保留。2026-09-14 随后完成模型协议有限纠正、解释证据边界及纠偏指引的针对性修复，全量 Go、app race、构建和严格校验通过；在这次修复完成时，后续第四、第五批尚未运行。详见[修复记录](../../../../docs/verification/2026-09-14-targeted-agent-fixes.md)。

历史局部复测按用户要求只运行两个关键错误案例各一次：`product/1` 通过；`missing_output/3` 仍有一项关键解释错误。该次未运行其他场景，不改变完整验收状态，见[局部复测](../../../../docs/evaluations/2026-09-14-critical-cases-retest.md)。

随后完成[缺文件反馈证据修复](../../../../docs/verification/2026-09-14-failure-evidence-fix.md)并运行第四批完整 84 例：关键错误 0，但连续组意图和策略均为 21/24（87.50%），总体未通过，见[第四批报告](../../../../docs/evaluations/2026-09-14-fourth-v3-results.md)。再次完成[输入版本绑定修复](../../../../docs/verification/2026-09-14-input-binding-fix.md)及诊断后运行第五批；自动检查 83/84 通过，344 次模型调用、69 次固定 Provider 提交、0 次真实 Tripo 调用，耗时 341.437 秒，75 个冻结源码摘要保持不变。完整汇总已离线等值核对；该批原始关键错误与连续组解释失败按旧评分版本保留，详见[第五批报告](../../../../docs/evaluations/2026-09-14-fifth-v3-results.md)。删除视觉评分时未自动标记完成；此后依用户追加授权，完成独立全量离线审核并达到新范围门槛，才更新当前验收状态。
