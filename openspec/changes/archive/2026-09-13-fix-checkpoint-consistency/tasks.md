## 1. 固定 Eino 恢复协议与旧数据验证依据

- [x] 1.1 使用锁定的 Eino v0.9.19 和受控模型建立最小协议集成测试，覆盖完整助手响应重放、`StatefulInterrupt`、真实 `Set` 和 `Runner.Resume`，断言根 key 不变、工具调用 ID 与原提议的 `reasoning_content` 保留；验证：正常恢复通过，必需字段丢失或悬空 Tool Call 被明确拒绝，对应 F15。
- [x] 1.2 基于 `b91ab83` 的旧数据格式与真实 Eino 中断生成临时兼容夹具，包含旧字符串暂停、Ready 未更新、答案被清除、已知任务和未知提交；验证：夹具能稳定复现旧问题且不读取或修改当前 `data/`，为 F12 提供迁移前断言。

## 2. 增加恢复记录及存储边界

- [x] 2.1 为会话定义恢复协议版本、已提交恢复点、待提交暂停、有限重建材料和按等待 ID 保存的答案，明确逻辑暂停身份与提交代次的区别；验证：序列化往返保留绑定关系，旧 JSON 可兼容读取，`Session.View()` 不暴露内部恢复数据。
- [x] 2.2 为检查点增加身份、代次、摘要及协议版本元数据，并实现同事务更新检查点、必要会话字段和事件的存储入口；验证：中间报错整体回滚，同内容重复提交幂等，旧代次、跨会话和不同内容提交被拒绝，对应 F02、F04、F13、F14。
- [x] 2.3 实现可重复运行的增量 schema 迁移及随会话删除的清理逻辑，保留旧检查点字节、归属、预算、任务 ID 和文件；验证：对旧夹具连续迁移及迁移中退出后重启均不丢记录、不重复计数，对应 F12、F14。

## 3. 将暂停发布改为原子提交

- [x] 3.1 将澄清和生产准备改为只持久化已验证的待提交草案，暂不发布新问题、计次、入队或覆盖已提交的 `Current`；验证：prepare 完成时外部不能回答新问题或调度新生产，已有纠偏名额和原操作结果仍保留，对应 F01、F04、F06。
- [x] 3.2 为一次 Runner 执行共享暂停提交协调器，在 Eino 的 `Set` 回调内校验最新状态并原子发布暂停、计数、队列/名额和事件，遵守现有锁顺序；验证：重复回调无重复业务效果，并发停止、到期和名额认领不会被旧快照覆盖，对应 F02、F03、F11、F14。
- [x] 3.3 移除回答、消费循环及正常调度对 `CheckpointReady` 的依赖，统一读取并校验已提交恢复点；验证：提交后通知前退出仍可回答，回答先于消费循环结束也能保留，保存失败后的中断事件不能发布成功状态，对应 F03、F05。

## 4. 实现尚无新检查点时的受限重建

- [x] 4.1 在准备暂停时关联并保存该次决策的完整输入、助手响应和唯一工具提议，保留协议字段并校验参数与身份；验证：可区分不同决策下重复的工具调用 ID，提议与草案不匹配时拒绝准备，材料不进入公共视图或事件导出，对应 F13、F15。
- [x] 4.2 实现只返回一次已保存提议的恢复模型适配器，使用真实 Runner 重建同一暂停并进入原子提交路径；验证：首次无检查点和后续存在旧检查点均恢复同一草案，不重复系统提示、不访问真实模型、不增加模型调用或生产次数，对应 F01、F04、F14、F15。
- [x] 4.3 为重建材料错误、额外模型调用及检查点写入错误实现明确原因与有界恢复处理，阻止调度循环无限重试；验证：参数变化立即拒绝，短暂写入失败可继续同一草案，重试耗尽明确结束或报告存储错误，对应 F02、F13。

## 5. 保留答案与已完成操作的重放证据

- [x] 5.1 将已接受答案按当前等待 ID 持久保存，澄清工具恢复读取原答案而不删除，展示字段与答案证据分离；验证：接受答案后及工具返回后重启均无需再次输入，多轮答案不串用、澄清计数不增加，对应 F05。
- [x] 5.2 仅在下一暂停原子提交时替换当前操作，以原操作身份去重产物和完成事件，完成操作重放返回保存的报告与最新持久预算；验证：文件写入、产物保存及工具返回附近退出后仍只有一份产物记录，业务报告不变、预算不回退，对应 F04、F10。
- [x] 5.3 在恢复工具入口校验暂停令牌与当前操作阶段，保留未知提交不重发、已知任务续查和已完成结果复用；只允许恢复重入时的相同已确认意图返回原结果；验证：重放不改变约束、生产次数或截止时间，不相关工具不会由暂停重建触发，对应 F08、F09、F10。

## 6. 接入启动对账与兼容恢复

- [x] 6.1 在接纳新请求和正常调度前对持久恢复状态对账，按设计优先级处理终止、期限、任务、草案和已提交恢复点，并在执行前复核；验证：已接受队列保留原顺序，`queue_full` 不自动入队，停止和到期不复活，预算与名额不重置，对应 F06、F07、F11。
- [x] 6.2 通过受限兼容 Runner 和公开中断状态验证旧检查点身份，匹配后重新中断并保存新协议；仅迁移可唯一关联的旧答案与重建材料；验证：Ready 缺失可恢复，错配或不可逆丢失答案明确失败，重复迁移不重复提问或入队，对应 F12、F14。
- [x] 6.3 在已知任务缺少可用 Agent 检查点时执行原任务的确定性查询、下载和技术检查，并依据报告给出程序说明；验证：即使模型额度耗尽仍在原期限内处理原任务，不调用模型纠偏或新增 Tripo 提交，未知提交仍停止，对应 F08、F09、F12。
- [x] 6.4 增加最少的恢复提交、重建、拒绝事件及原因码，隔离单个会话恢复失败，沿用匿名归属与脱敏规则；验证：损坏记录不阻塞其他会话，公共视图、WebSocket 事件和导出均不含原始检查点、完整协议消息或凭证，对应 F13。

## 7. 覆盖真实进程退出与并发边界

- [x] 7.1 建立仅存在于测试中的子进程入口、持久化屏障和父进程受控 Provider，使用临时 SQLite 与模型目录，在指定位置强制终止子进程；验证：终止不经过 `Service.Close()`，父进程可连续核对重启前后的实际提交次数与任务状态，服务无新增故障注入配置。
- [x] 7.2 组合运行首次/后续暂停、事务中退出、提交后通知前退出和答案读取前后退出的回归；验证：逐项满足 F01–F05、F14、F15，原问题及答案可继续，未出现重复计数、协议损坏或重新调用模型决定同一暂停。
- [x] 7.3 组合运行队列、名额、未知提交、已知任务、文件落盘和产物保存的退出回归；验证：逐项满足 F06–F10，队列与额度保持，远端提交不重复，报告与产物不重复。
- [x] 7.4 组合运行停止/到期竞态、兼容迁移中退出、损坏检查点及多会话恢复回归；验证：逐项满足 F11–F14，较新持久事实不被覆盖，不可恢复请求有明确原因，其他会话能完成恢复。

## 8. 完成回归与变更证据

- [x] 8.1 在恢复专项通过后运行 `go test -race ./... -count=1` 与 `go vet ./...`，检查既有澄清、预算、匿名隔离、队列和 DeepSeek 协议回归；验证：全量检查通过，或将环境阻塞与实现失败分开记录，不能把未执行项记为通过。
- [x] 8.2 实现验证完成后记录恢复协议 ADR、F01–F15 验证结果和迁移/回退边界，按需更新稳定项目上下文；验证：文档与最终实现一致，明确未调用真实付费 API，保留完整 Agent 案例集的独立验收状态，不自动归档前一变更。
