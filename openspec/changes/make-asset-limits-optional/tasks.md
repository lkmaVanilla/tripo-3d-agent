## 1. 可选约束模型与兼容表示

- [x] 1.1 为新版工具定义继承、设置、取消三态输入及可空正式上限，建立旧协议适配；用序列化/反序列化测试验证缺失、null、正数、零、负数、溢出与模式冲突，且旧版工具 schema 保持兼容。对应 optional-asset-constraints“技术验收上限独立可选”。
- [x] 1.2 实现明确变更优先、来源继承其次、无来源留空的归一化及来源记录；用测试覆盖两个上限独立设置、只取消一个、继承旧版来源不明数值、不从实测值生成上限。对应“约束变更区分继承设置与取消”。
- [x] 1.3 将空值、来源及最小减面目标模式纳入意图草案、正式保存、版本来源与上下文投影；验证草案不授权生产、取消不被补回、正式接受后改写被拒绝，以及保存再读取保持一致。对应 continuous-asset-conversation。

## 2. 版本化 Agent 指引与工具协议

- [x] 2.1 新增 v4 执行 profile、工具 DTO 与四份 Skill，移除新版强制默认，明确非必要不追问、生产参数非验收约束、相对减面和技术解释规则；以 profile/schema/Skill 加载测试验证新协议并校验 v1/v2/v3 原 Skill 哈希未变。对应 agent-audience。
- [x] 2.2 梳理并替换仅识别 v3 的连续创作分支为有限版本能力判断，覆盖工具包装、输入绑定、父关系、内部上传、回答结局与投影；用同会话 v3/v4 混合测试验证未丢失引用隔离、零生产回答和不可变历史保护。对应 agent-audience、continuous-asset-conversation。
- [x] 2.3 让新的聊天目标使用 v4，旧单请求入口继续 v2，既有 Run 不升级；入口测试验证新会话、旧会话后续目标、兼容入口及恢复分别选择正确版本。对应“提示升级保留在途会话的执行版本”。

## 3. 技术检查与正式结果

- [x] 3.1 为新版技术检查增加可选上限与 not_applicable，始终执行文件检查和可信测量；参数化测试覆盖四种上限组合、边界等值、6000 面/12 MiB 无约束合格产物、损坏文件和不支持格式。对应 verified-results。
- [x] 3.2 更新报告完整性校验与正式结果生成，共用版本化判定规则；测试必须拒绝有上限意图配空上限报告、伪造状态/实测值、无效文件及不一致版本，旧报告仍保持原结论。对应“正式结果以实测证据为依据”。
- [x] 3.3 更新输入评估、产物检查、版本报告及执行核验对可选上限的使用；集成测试验证新目标重评不覆盖旧报告、合法未达用户上限候选仍保留、只对适用上限判超标。对应 verified-results、continuous-asset-conversation。
- [x] 3.4 保留下载和解析保护并区分资源限制原因；用边界测试验证 150 MiB 下载限制、256 MiB accessor 展开保护不因取消上限失效，正式结果不称其为用户上限失败或伪造完整测量。对应“系统资源保护独立于用户验收约束”。

## 4. 生产、减面与恢复

- [x] 4.1 统一正常生产和恢复重建的目标参数校验：保留 500–20000 目标范围，有上限时才比较；测试无上限 8000 目标可执行、有上限越界被拒绝、缺少目标参数被拒绝及用户小于工具范围时不擅自放宽。对应“生产参数不充当验收上限”。
- [x] 4.2 保存并校验减面目标模式和输入证据，将无需加工、纠偏及交付条件与技术通过分开；测试无上限进一步减面、输入已满足绝对上限但仍要求降低、仅达上限已满足零生产、输出未减少不交付、无可行目标不提交。对应“无绝对上限的减面仍需证明目标完成”。
- [x] 4.3 接通可选约束下的生成、下载、检查、有限纠偏和正式交付；受控端到端测试验证无上限超旧默认直接交付，单项明确上限超标仍纠偏，不能通过删约束或改目标绕过失败。对应 optional-asset-constraints、verified-results。
- [x] 4.4 覆盖 v4 澄清暂停、排队/待提交重建、已知任务恢复和相对减面恢复；测试空值、来源、操作参数、输入身份、预算和截止时间保持，原操作不重复提交，未知提交不重发。对应 agent-audience 与现行 checkpoint-recovery 规则。
- [x] 4.5 回归旧 v1/v2/v3 的澄清、生产、已知任务检查和 Skill 哈希恢复，并覆盖旧上限继承后显式取消；用冻结 fixture 验证旧报告、种子序列化和执行身份不被升级改写。对应“提示升级保留在途会话的执行版本”。

## 5. 接口与网页最小适配

- [x] 5.1 对 HTTP 快照、WS 实时/补读、版本与导出统一投影可空上限和不适用状态；接口测试验证同一 Run 数据一致、旧接口兼容、历史未知不被投影成无约束。对应“输出接口共享按执行核验的正式结果”。
- [x] 5.2 调整计划、需求差异、技术检查及版本详情中的空值和状态展示，区分未设置、取消、继承、不适用与未知；网页测试验证真实数据渲染和刷新/断线后恢复，不新增页面或配置表单。对应 verified-results。

## 6. 回归与 Agent 评测证据

- [x] 6.1 完整运行 Go 测试、竞态检查及现有网页测试，确认预算、并发、隔离、不可变版本与恢复系统回归全部通过；保存命令、环境与结果，不以 OpenSpec 校验替代应用测试。
- [x] 6.2 更新当前 20+8 案例的 v4 预期和固定供应商响应，保留原案例身份与纠偏覆盖；增加无上限、单上限、明确取消、继承和相对减面语义案例，逐项列出预期、适用评分项及关键错误标准，审阅确认无视觉评分、无空值默认伪装或削弱明确约束的断言。对应“可选约束按非视觉行为独立评测”。
- [x] 6.3 对新版执行运行既有两组各三次的 84 次真实 LLM 固定供应商评测，补充案例各运行三次并独立汇总；保留全部原始结果及语义审阅，分别验证关键错误为零、其余适用维度不低于 90%，失败修复和复测不覆盖原记录。
- [x] 6.4 完成一次无技术上限生成与一次同会话相对减面的真实 Tripo 有界冒烟；保存版本父关系、真实输入输出、报告、实际面数变化和正式结果。缺少凭证或额度时记录未验证且保持本任务未完成，不以受控响应替代真实证据。

## 7. 文档与交付核验

- [x] 7.1 实施时更新 CONTEXT 中默认技术上限、默认假设、几何规模、文件体积及相关术语，并补后继 ADR 说明对 ADR 0003/0012 的局部替代；核对 PROJECT_BRIEF、README 与 OpenSpec 背景引用一致，历史归档及验证记录不重写。交付可审阅文档差异。
- [x] 7.2 编写升级、备份、混合版本读取和安全回退说明，在隔离数据副本验证升级后历史保留及 v4 正常读取；说明旧二进制不能直接处理 v4，不自动删除新数据。
- [x] 7.3 执行 OpenSpec 严格校验并形成规格—测试—证据对照表，核实四个能力增量均有验证覆盖，明确计划、已实现、已验证的区别；未执行的真实评测和供应商验证保持未完成。
