# Tripo Agent

面向 3D 创作者及行业从业者的 3D 资产创作 Agent，优先承担资产制作助手的角色，通过网页向用户提供服务。用户提供需求、素材或已有资产与目标，由 Agent 组织制作过程，交付可使用或继续加工的资产。

## 长期目标与阶段范围

长期能力方向包括资产生成、资产处理、骨骼绑定和动画，按阶段逐步扩展。长期任务输入方向包括文字、图片或已有模型；制作操作及输入条件始终以 Tripo API 实际开放的能力为边界，具体版本和验收条件另行确定，未实现的方向不构成当前能力承诺。

Tripo API 是项目长期唯一的资产生产与加工能力来源，不以接入其他 3D 服务或本地建模工具补齐其能力缺口。这一边界针对资产制作操作，Agent 的模型服务、应用调度、下载、技术检查和网页展示仍由各自组件承担；Tripo 已开放的能力也必须经过本项目接入与验证，才能作为可用能力。技术取舍见 [ADR 0020](docs/adr/0020-tripo-only-asset-production.md)。

长期采用围绕同一资产持续协作的会话：一个制作目标完成后，用户可以在原会话中基于已有资产结果提出后续目标，继续与 Agent 创作。例如，在相关能力完成接入后，可先生成角色，再追加骨骼绑定和动画制作目标。会话的交互生命周期与一次目标的执行结束分别表达，方向见 [ADR 0021](docs/adr/0021-continuous-asset-collaboration.md)。

当前 MVP 用文本到静态资产的端到端链路验证 Agent 的基础制作能力。独立游戏开发者的静态道具需求是首个验证场景，木箱是具体演示案例；该场景不限定整个项目的长期服务对象或资产类型。

本文件中“首版”与“MVP”指第一阶段静态资产基础链路，其原有评测门槛仍保留。当前连续创作阶段增加同会话后续目标、显式版本引用和聊天工作台；图片、用户模型导入、绑定和动画仍未接入。首条演示成功、阶段验收通过与长期目标达成分别记录。长期演进方向见 [PROJECT_BRIEF.md](PROJECT_BRIEF.md)，当前实现与验证证据见 [README.md](README.md)。

## 明确排除

- 多 Agent、通用 Agent 平台、RAG、跨会话长期记忆和动态安装 Skill；同一资产会话中的创作上下文可随后续目标延续。
- Kubernetes、GPU 或推理基础设施、训练或微调 3D 模型。
- 复杂前端、替代 Tripo Studio。
- 接入其他 3D 生产服务或本地建模工具，以提供 Tripo API 未开放的资产制作操作。

## 当前 MVP 能力边界

- 首版限定文本输入和静态道具 GLB 交付；图片输入、已有资产导入、骨骼绑定和动画属于后续阶段方向，尚未实现。
- 首版仅采用技术检查，排除自动视觉检查、外观符合性保证，以及减面和重新生成之外的自动纠偏能力。新能力的验收要求需在相应阶段明确。
- 当前会话围绕同一资产主题顺序接受多个目标。每条正式新目标建立独立 Run；当前 Run 的澄清答案继续原执行。运行、排队、队列满和退出中不能发送下一目标，只保留草稿。
- 加工只能引用当前会话生产的一个明确版本；预览不等于引用，不能自动选最新版本。每个新产物追加版本，不覆盖旧文件和报告；三轮后仍无合法输入时零生产结束。

## 首条端到端演示

用户输入：

> 给我的俯视角游戏原型做一个低模木箱。

澄清后确定本次请求的要求：卡通静态木箱、GLB 格式、不超过 5,000 个三角面、文件不超过 10 MiB。这两个数值是早期演示与旧版执行的历史参数。新版聊天目标按用户明确要求或引用版本继承，未设置时不自动补全（ADR 0023）。

演示链路：

1. 展示结构化意图、约束和 Agent 计划。
2. 真实调用 Tripo，通过 WebSocket 展示异步进度。
3. 下载产物，执行文件有效性、几何规模和文件体积三类技术检查。
4. 检查失败时，由 Agent 在既定预算内选择减面、重新生成或停止。
5. 展示模型预览与下载、技术检查报告、完整执行记录及单次执行评测结果。

纠偏是否发生由实际检查结果决定；首次产物合格就直接交付。最终未通过检查时，依照既定终止规则解释未满足的技术要求与停止原因。

## 执行追踪与证据展示

每个资产请求采用按顺序排列的事件时间线，页面默认展示摘要，展开查看证据详情，并支持导出完整的脱敏 JSON 执行记录。

| 内容 | 记录范围 |
|---|---|
| 需求与计划 | 原始需求、澄清、确认意图、默认假设及计划变化 |
| Agent 决策 | 选择的动作、简短依据、引用的检查结果、加载的 Skill |
| Runtime 处理 | 动作放行或拦截、预算变化、排队、恢复和停止 |
| 工具执行 | 脱敏后的参数与响应、任务 ID、异步进度、耗时和错误 |
| 产物与评测 | 实测面数和体积、检查结论、评测规则及对应证据 |

同时记录本次使用的模型、Prompt 和 Skill 版本。Agent 提议、Runtime 处理和实际工具结果分别记录并关联，不能将计划或决策说明当作动作已经执行的证据。

持久化记录为页面展示、断线重连、JSON 导出和评测提供共同依据。记录与导出按匿名会话归属校验访问权，脱敏范围包含密钥和访问凭证。

## 首版评测验收门槛

| 项目 | 门槛 |
|---|---|
| 测试规模 | 20 个固定案例，每例运行 3 次，共 60 次；使用真实 Agent、LLM 和固定 Tripo 测试响应 |
| 关键错误 | 60 次中不得出现超预算提议、擅自放宽硬约束或伪造技术验证结论 |
| 其他维度 | 意图与澄清、策略选择、结果解释，各自适用检查项的通过率不低于 90% |
| 结果报告 | 保留全部运行结果，逐项列出失败，不只展示最好的一次 |

每个案例预先明确适用检查项及允许行为，各维度分别以通过项数除以适用检查项总数计算通过率。当前采用非视觉评测范围，不评外观符合性、颜色材质、产物实际风格、部件完整、旧模型外形归因及是否声明未做视觉检查；技术数据、操作事实、预算、输入授权与技术因果解释仍需有依据；原文风格与用途的保留、用户要求与 Agent 默认的来源区分仍属于非视觉意图检查。关键错误出现一次即未达到验收门槛，不能通过其他项目的高分抵消；Runtime 成功拦截违规动作，仍算 Agent 决策失败。

并发、会话隔离、重启恢复和预算拦截另做系统回归检查，要求全部通过。Agent 评测、系统回归及真实 Tripo 完整链路验证分别提供证据，不相互替代。

## 首版产物与参数范围

首版接受文本输入，生成一个静态道具，以自包含的 GLB 单文件交付。文件内部允许多个网格，几何规模按所有网格的总三角面数检查。

| 项目 | 范围 |
|---|---|
| Agent 可调整 | 生成描述、目标面数、贴图质量；须满足用户硬约束及所选模型的参数限制 |
| 程序固定 | Tripo 模型版本及其他参数；关闭四边面、显式分件，不请求网格压缩 |
| 实际产物支持 | 自包含的静态 GLB、三角面网格；不支持压缩网格或外部资源依赖 |

生成目标与验收上限分别保存：Agent 可以降低目标面数，但不能提高用户已经确定的验收上限。固定配置的具体版本和值在实现时明确记录，不由 Agent 自行改变。

请求参数不能保证实际产物符合支持范围。遇到压缩网格、外部资源依赖或不支持的格式时，明确报告不支持或无法验证，不能跳过检查后宣布合格，也不引入独立格式转换工具。

## 可选技术验收上限

面数与文件体积上限分别可选。新聊天目标仅采用用户明确要求或所选版本的适用来源约束，无来源时保持未设置，不补 5,000 面或 10 MiB。未提出新要求时继承，用户明确取消时仅取消新目标的对应约束；当前执行正式接受后保持固定。

文件有效性始终检查，实际面数和体积仍然测量；无上限的比较项为不适用。生产目标面数和系统资源保护不构成用户验收上限。进一步减面还需要真实面数降低的证据，不能因输入通过技术检查就认定无需加工。决定见 [ADR 0023](docs/adr/0023-optional-asset-acceptance-limits.md)。

旧执行及旧单请求兼容入口继续采用其已冻结规则和默认值，历史报告不重写。本节描述的新聊天规则不改变首条木箱演示的历史参数，也不意味着新版评测已经完成。

## 匿名会话与并发访问

首版支持无需注册或登录即可创建会话，支持建立多个会话及并发访问。不同会话可以同时交互和推进各自的资产请求，不采用整个服务只能存在一个未结束请求的限制。

当前会话可顺序承载多个资产目标，每个目标的澄清、生产、纠偏和收尾属于同一个 Run。会话内最多一个活动 Run；用户停止后仍需等待 worker 退出才能接受新目标。制作另一个独立资产时新建会话。例如，制作木箱的会话与制作路灯的会话可以同时执行。

会话内的澄清、首次生成和纠偏沿用同一请求的 Context、预算和执行记录，不能把纠偏标记为新请求来重置额度。

不同会话的对话与执行记录分别组织；不同资产请求的 Context、预算、检查点、任务状态及进度事件相互隔离。既定的澄清轮次、生产次数、模型调用次数和执行期限仍按资产请求分别计算。

单 Agent 指每次资产请求由一个 Agent 负责决策，不限制整个服务只能运行一个请求。同一资产请求内部的生产操作仍串行执行，不同会话可以并发推进。

## 生产并发与等待队列

| 项目 | 首版行为 |
|---|---|
| 生产并发 | 同时最多 3 个处于生产阶段的资产请求，覆盖生成、异步等待、下载、检查和纠偏 |
| 等待队列 | 最多 10 个请求，按入队顺序自动开始生产 |
| 队列已满 | 提示繁忙，保留需求，允许稍后重试入队 |
| 等待用户澄清、查看结果 | 不占生产名额 |
| 排队期间 | 不消耗生产提交额度，不启动 30 分钟执行计时 |

3 和 10 是可配置初值，接入时依据实际 Tripo 账号配额核对。生产名额约束应用中的生产阶段请求数，不代表远端任务并发数；本地停止后，远端已提交任务仍可能运行。

Go 执行层管理入队和调度，队列状态及顺序随请求持久化。同一请求只允许一个有效队列条目；用户停止排队请求后不再自动启动。恢复时先核对已有生产请求，再按原队列顺序调度等待请求，保留原有预算和截止时间。

## 匿名访问与会话恢复

服务端自动签发随机匿名访问凭证，浏览器通过持久 Cookie 保存，无需用户注册或登录。同一匿名访客可以创建多个会话，服务端记录会话归属，并对会话列表、对话、进度、模型下载和任务控制校验该归属。

| 情况 | 行为 |
|---|---|
| 同一浏览器刷新或重新打开，凭证仍有效 | 可以找回此前创建的多个会话 |
| 其他访客获得会话链接 | 不能仅凭链接查看对话、下载模型或控制任务 |
| 更换浏览器、清除凭证或凭证过期 | 首版不提供旧会话找回或跨设备恢复 |

匿名凭证识别访问归属，不建立真实个人账号。失去访问凭证不等于后台任务停止或数据已删除；后台执行继续遵守既定预算和终止规则，数据按保留期限清理。

## 匿名会话数据保留

当前连续创作以整个会话为保留和删除单位；单个 Run 的原始结束与到期时间保留为历史证据。具体取舍见 [ADR 0022](docs/adr/0022-conversation-runs-and-immutable-versions.md)。

| 对象 | 首版规则 |
|---|---|
| 匿名访问凭证 | 连续 30 天未访问后失效，正常访问时滚动延长有效期 |
| 尚未开始生产的 Run | 连续 24 小时无用户操作后结束，涵盖等待澄清和排队 |
| 空闲会话 | 自最后一个 Run 的持久化结束时间起整体保留 7 天 |
| 到期前正式接受新目标 | 保护整段会话，待新 Run 结束后重新计算期限 |
| 活动或退出中的 Run | 会话及历史文件不清理 |
| 浏览、下载、心跳、草稿 | 不延长会话保留期 |

这些期限作为可配置初值。页面显示数据到期时间，支持用户提前下载模型和导出执行记录；固定评测用例与正式评测报告作为项目资料单独保存，不纳入线上匿名会话的自动清理。

后台轮询和心跳不延长生产前的 24 小时等待期限。结束后浏览不续期；只有正式接受的新目标会保护整段会话并在结束后重算期限。到期后先阻断全部新旧入口，再幂等删除所有关联文件和记录。迁移旧数据不重置原期限；重启不复活已停止的 Run。

## 已确认运行时架构

应用以单个 Go 服务运行，使用 Eino ADK 的 `ChatModelAgent` 和 `Runner` 承担模型与工具决策循环、澄清时暂停及收到回答后恢复，以及运行事件输出。

Go 业务执行层负责约束校验、预算扣减、生产并发与队列调度、Tripo 任务跟踪、轮询、下载和技术检查。事件记录与推送汇总模型决策、工具执行及任务进度，用于执行追踪、评测和 WebSocket 展示；Eino 检查点所需的存储由应用提供。

同一资产请求内的生产操作串行执行，下一次纠偏依据上一次技术检查结果；不同会话的资产请求可以并发推进，执行状态相互隔离。Eino 的模型循环次数限制与业务上的澄清轮次、生产提交次数分别控制，不能相互替代。

## Agent 模型服务

Agent 的大语言模型采用 DeepSeek V4 Pro，API 型号为 `deepseek-v4-pro`，承担意图理解、需求澄清、计划与工具决策，以及结果解释。3D 生产由 Tripo 完成，产物技术检查由 Go 执行层完成。

首版以该型号作为 Agent 演示和案例集评测的模型基线。Eino 适配器的接入路径及验证事项见 [ADR 0008](docs/adr/0008-single-go-service-with-eino-adk.md)。2026-09-08 已完成首条真实木箱链路验证，证据见 [演示验证记录](docs/demo-verification.md)。此后完成协议与解释、缺文件反馈证据和输入版本绑定修复，并保存全部五批评测历史。2026-09-14 按用户要求移除视觉相关评测，并获授权对第五批完整 84 例、344 条原始提议采用 `technical-only-v1` 独立重新审核：两组关键错误均为 0，各适用维度均达到 90%，见[新审核报告](docs/evaluations/2026-09-14-technical-only-reassessment-results.md)。本次零新增模型/Tripo 调用，旧成绩保留，六项非关键失败公开记录。结合[系统回归 8/8](docs/verification/input-binding-acceptance/system-checks.md)、评分范围调整后的本地回归及既有浏览器、真实供应商与恢复证据，任务 `10.6`、`11.2` 已完成，进度 47/47；详见[阶段验证](docs/verification/conversation-workspace.md)。当前变更的 6 个能力规格已同步为[主规格](openspec/specs/)，变更已于 2026-09-14 [归档](openspec/changes/archive/2026-09-14-add-conversational-asset-workspace/)。这仅完成当前阶段，图片输入、绑定、动画等长期能力仍待实现。

## 执行停止规则

| 边界 | 行为 |
|---|---|
| 单请求累计最多 20 次模型调用，失败尝试也计数 | 停止模型决策和新增生产；已提交任务仍在执行期限内查询、检查，由程序输出结果说明 |
| 从首次生产提交开始，最多执行 30 分钟 | 包含轮询、下载、检查和重启停机时间；到期停止本地自动执行 |
| 尚未开始生产，连续 24 小时无用户操作 | 结束请求，排队中的请求不再自动启动 |
| 用户主动停止 | 停止本地后续执行，保留已有结果、Tripo 任务 ID 和执行记录 |
| 刷新或服务重启 | 不重置调用计数和截止时间，不自动恢复已终止请求 |

等待用户澄清及首次生产前排队的时间不计入上述 30 分钟。20 次和 30 分钟是首版可配置初值，后续依据真实演示耗时调整；当前请求按已保存的上限和截止时间执行，Agent 不得自行延长。

Go 应用持久化模型调用计数、绝对截止时间和终止状态，并强制执行边界；失败调用及重试尝试不能绕过调用额度。模型调用额度耗尽与执行终止分开处理：前者保留已提交任务在期限内的确定性查询与验收，超时或用户停止则结束本地自动执行。停止本地执行不能被展示为 Tripo 远端任务已经取消。

## Context 与 Skill 范围

Context 每轮提供当前资产请求的原始需求、已确认意图、默认假设、计划、剩余预算、关键工具结果和必要对话。完整执行日志保存在 SQLite；硬约束与预算从结构化状态读取，不能依赖模型回忆或摘要重建。

v1/v2 保留三份随版本冻结的只读 Skill，聊天 v3/v4 增加资产加工指引，v4 使用可选技术上限协议。Skill 按需加载，由同一个 Agent 执行：

| Skill | 内容 |
|---|---|
| 需求理解与澄清 | 提取用途、识别约束和冲突、明确默认假设 |
| Tripo 生成 | 将意图转成生成描述及适用参数 |
| 技术纠偏 | 根据检查结果、工具条件和预算选择减面、重新生成或停止 |
| 资产加工（v3/v4） | 锁定本会话真实输入，区分减面与文本再生成，保留旧版和来源 |

当前会话可承载多个顺序目标。每个新 Run 冻结当前原文、资产主题、明确版本及来源意图和报告、最近最多 20 条公开消息和 5 次结束摘要；自由消息正文最多 12,000 个 Unicode 字符，裁剪有标记，引用版本事实不受窗口限制。引用版本后的意图先保存需求差异草案，再在同一次工具调用内冻结正式意图；草案不授权生产，也不要求额外用户确认。未提供字段承接来源，明确新值由 Agent 从本次用户语义提取，Go 如实记录字段差异。每个 Run 的 Eino History 与 checkpoint 独立，公开聊天从已接受业务事实重建。这不扩展为跨会话长期记忆，也不引入动态安装 Skill。

## 持久化方案

| 存储 | 保存内容 |
|---|---|
| SQLite | 匿名凭证校验信息与有效期、会话归属、会话与请求关联、用户操作时间与数据到期时间、对话、意图、预算、排队状态与顺序、生产阶段状态、模型调用计数、执行截止时间与终止状态、生产操作、Tripo 任务 ID、Eino 检查点、执行事件、模型与 Prompt/Skill 版本及评测结果 |
| 本地持久目录 | 下载的模型文件；数据库记录其路径和元数据 |

预算扣减、生产操作记录和相关事件在同一数据库事务中保存。Tripo 调用和模型文件写入不属于数据库事务，需要按既定恢复规则核对状态；部署时为数据库和模型目录配置持久磁盘。

## 断线与重启恢复边界

| 情况 | 行为 |
|---|---|
| 浏览器刷新或 WebSocket 断线 | 后台任务继续执行；凭证仍有效时，重新连接后恢复展示进度与记录 |
| 服务重启，正在等待澄清回答 | 从已保存检查点恢复，保留已确认需求与澄清轮数 |
| 服务重启，正在等待生产名额 | 保留原队列顺序，核对已有生产请求后继续调度；不因恢复重复入队 |
| 服务重启，未开始生产且已连续 24 小时无用户操作 | 记录终止，不再恢复澄清或启动生产 |
| 服务重启，已有 Tripo 任务 ID | 请求未终止且未到截止时间时，继续查询原任务、检查产物；调用额度允许时再进入后续决策 |
| 提交结果未知，无法确认任务是否创建 | 停止自动执行并解释原因，不自动重复提交；保留已占用的生产额度 |
| 请求已终止或恢复时已到截止时间 | 保留结果与记录，不自动恢复执行；到期请求记录超时终止 |

请求状态、预算和必要的检查点需要持久化。所有恢复沿用原请求的约束、已消耗预算、模型调用计数、执行截止时间和执行记录，不能因重启而重置限制或把同一次请求变成一次新的生成。恢复规则仅适用于尚未终止的请求。

## Language

### 资产与需求

**用户（User）**：
向 Agent 提出资产制作目标、使用或继续加工交付资产的 3D 创作者或行业从业者。独立游戏开发者是首个验证场景中的用户，观看项目演示的人不因此被视为目标用户。

**资产制作助手（Asset Creation Assistant）**：
围绕用户的资产目标，在 Tripo API 制作能力范围内组织生成与后续处理、依据实际结果决定下一步并交付资产的 Agent 角色。当前优先按制作目标委托任务；具体操作进入哪个阶段，取决于 Tripo 的开放能力和项目验收。

**当前 MVP（Current MVP）**：
以文本到静态资产制作为范围、验证 Agent 基础能力的首个阶段，验收包含端到端链路、系统回归与案例集评测。该阶段的能力边界不等同于项目的长期能力上限。

**端到端演示案例（End-to-End Demo Case）**：
用于展示和验证一个具体资产请求从输入到交付全过程的案例。木箱是首个案例，单个案例成功不能替代整个阶段的验收。

**匿名访客（Anonymous Visitor）**：
由匿名访问凭证识别的访问主体，无需注册或登录即可创建多个会话。该身份用于会话归属，不代表经过验证的真实个人。

**匿名访问凭证（Anonymous Access Credential）**：
服务端自动签发、由浏览器持久保存并用于校验会话归属的随机凭证，首版连续 30 天未访问后失效。凭证仍有效且会话数据尚在保留期内时，可在同一浏览器找回已有会话；更换浏览器、清除凭证或凭证过期后，首版不提供旧会话找回。

**会话（Conversation）**：
归属于匿名访客、围绕同一资产主题组织多个目标、公开消息及版本的交互单位；一次目标结束后仍可继续协作。制作另一个独立资产时新建会话。
_Avoid_：把会话与单次执行混用。

**单次执行（Run）**：
一次正式接受的用户目标及其独立预算、澄清、生产操作、结果和恢复记录；同会话一次只推进一个 Run。澄清答案与纠偏不创建新 Run。

**持续资产协作（Continuous Asset Collaboration）**：
用户在同一会话内基于已有资产结果与创作上下文，先后提出制作目标并与 Agent 继续创作的交互方式。当前支持文本生成、指定版本减面和技术解释；一次目标结束不自动等同于整个会话结束。

**资产请求（Asset Request）**：
用户交给 Agent 的一次目标及其约束，可涉及生成、加工或解释；每个正式目标对应一个 Run，澄清和纠偏共用其 Context、预算与执行记录。

**需求澄清（Requirement Clarification）**：
Agent 与用户围绕资产请求中的关键缺失、歧义或冲突进行的问答过程，在 Agent 判定关键需求明确或完成三轮时结束。三轮后可默认补足非关键缺失；仍有硬约束冲突或无法确定合法加工版本时，解释并零生产结束，不能默选输入。

**硬约束（Hard Constraint）**：
资产请求中必须满足、Agent 不得自行舍弃或放宽的要求。未解决的硬约束冲突指仍存在无法同时满足的硬约束。

**默认假设（Default Assumption）**：
为补全缺失信息而采用、并向用户明确展示的处理依据。默认假设不得覆盖或放宽已有硬约束。新聊天目标的面数、体积上限不属于默认假设；生产参数选择不得自动升级为用户硬约束。

### 产物与验收

**交付模型（Delivered Model）**：
用户针对资产请求最终获得的、可预览和下载的 3D 模型，首版限定为自包含的静态 GLB 单文件。

**候选模型（Candidate Model）**：
一次生成或减面操作得到的模型结果，是技术检查和后续纠偏的对象。

**结果纠偏（Result Correction）**：
围绕同一资产请求，根据技术检查的失败项、工具适用条件和剩余预算，在减面、重新生成或停止之间作出选择并记录依据的过程。每次处理后重新执行文件检查与测量，并比较所有适用的技术上限，始终保留用户原始约束；没有可行操作时停止并解释原因，额度耗尽只阻止新增生产操作，不跳过已提交操作的查询与验收。

**减面（Mesh Decimation）**：
对锁定的历史输入版本或本 Run 新候选降低三角面数的生产操作，既可用于后续目标，也可用于本目标纠偏。

**重新生成（Regeneration）**：
按文本要求生成新的几何，可用于允许的目标内纠偏或明确的新目标；引用旧模型仅承接背景，不表示直接消费旧几何，不能虚构几何父关系。

**生成目标面数（Target Face Count）**：
提交生产操作时使用的面数目标，与实际产物的三角面数和验收上限分别记录。Agent 可调整目标，但不得据此放宽验收上限。

**生产操作（Production Operation）**：
向 Tripo 提交的首次生成、重新生成或减面操作，按提交尝试计数。失败或结果未知的提交也占用次数额度。

**生产预算（Production Budget）**：
单次资产请求的生产操作次数额度：首次生成一次，减面和重新生成共用最多两次纠偏额度，总计最多三次提交尝试。Runtime 强制执行此上限，查询状态、下载和检查不占生产额度，但另受重试与超时限制。

**生产名额（Production Slot）**：
应用允许一个资产请求处于生产阶段的容量单位，首版默认同时最多 3 个。名额覆盖该请求的生成、异步等待、下载、检查和纠偏，直到完成或终止；等待用户澄清、排队和查看结果不占名额。

**等待队列（Production Queue）**：
已准备开始生产、正在等待生产名额的资产请求集合，首版默认最多 10 个，按入队顺序自动开始。队列满时保留需求并允许稍后重试入队；排队不消耗生产提交额度，也不启动执行计时。

**模型调用额度（Model Call Budget）**：
单次资产请求允许的累计模型调用次数，首版默认最多 20 次，失败及重试尝试也计数，澄清恢复和服务重启不重置。耗尽后禁止继续调用模型或新增生产操作，已提交任务仍在执行期限内查询与验收，由程序输出结果说明。

**执行截止时间（Execution Deadline）**：
由首次生产提交时间加执行时长上限得到的绝对时间，首版默认上限为 30 分钟。轮询、下载、检查及首次提交后的重启停机均占用这段时间，等待用户澄清和首次生产前排队不计入；到期停止本地自动执行，恢复不能延长截止时间。

**执行终止（Execution Termination）**：
当前资产请求结束本地自动执行的状态。执行超时、生产前连续 24 小时无用户操作或用户主动停止时，保留已有结果、Tripo 任务 ID 和记录，不自动恢复；本地终止不构成远端任务已取消的证据。会话整体期限由最后一个 Run 的结束时间计算。

**会话数据保留期（Session Data Retention）**：
空闲会话自最后一个 Run 结束后整体保留对话、所有版本、记录和文件的期限，默认 7 天；到期前正式接受新目标后重新保护整段会话，浏览不续期。固定评测用例与正式评测报告作为项目资料独立保存。

**提交结果未知（Submission Outcome Unknown）**：
已尝试提交生产操作，但无法确认外部任务是否创建的状态。此时停止自动执行并解释原因，不自动重复提交，保留已占用的生产额度。

**技术检查（Technical Check）**：
以实际模型文件及其可读取元数据为依据的确定性验证，是 MVP 唯一的自动产物验证方式，范围限于文件有效性、几何规模和文件体积。请求参数不构成产物验证证据，技术检查通过也不代表模型的物体类别、风格或外观符合用户描述。

**文件有效性（File Validity）**：
模型文件可解析且包含非空网格的性质。首版同时核对产物是否符合已声明的静态、自包含 GLB 和三角面网格支持范围；不支持或无法验证的产物不能记为检查通过。

**几何规模（Geometry Size）**：
实际模型文件内所有网格的总三角面数量，存在明确验收上限时进行比较；未设置上限时仍报告可信实测值。

**文件体积（File Size）**：
实际模型文件的大小，存在明确验收上限时进行比较；未设置上限时仍测量，下载和解析资源限制独立适用。

**验证反馈（Validation Feedback）**：
说明生成产物是否满足已检查技术要求的结论及其证据，不包含视觉符合性判断。接口调用成功本身不构成产物通过技术检查的证据。

### Agent 职责

**Agent**：
本项目中针对每次资产请求、根据该请求上下文作出策略决策的单个决策主体，决定继续澄清、启动生成、调整策略或结束任务。不同请求可以并发运行，单 Agent 架构不表示全服务只允许一次执行。Agent 不得修改检查结果或放宽验收上限来使产物通过检查。

**Agent Runtime**：
驱动本项目 Agent 决策循环的执行机制，管理等待用户、工具执行、预算和终止。

**Context**：
供当前资产请求每轮决策使用的工作信息集合，包含原始需求、已确认意图、默认假设、计划、剩余预算、关键工具结果和必要对话。硬约束与预算来自结构化状态，完整执行日志独立保存在 SQLite。

**Intent**：
资产目标、用途及约束的结构化表达，是生成和验收的依据。

**Agent 计划（Agent Plan）**：
Agent 向用户说明的生产与验收步骤安排，可依据反馈调整并记录变化。计划描述打算执行的动作，不构成动作已执行或产物已通过检查的证据。

**Skill**：
面向本项目资产任务、随仓库版本管理的只读指引与策略，由当前 Agent 按需加载并执行。当前 v4 包括需求理解、Tripo 生成、技术纠偏、资产加工；历史执行继续使用当时的冻结指引。

**Tool**：
输入输出明确的操作能力，例如提交生成、查询任务和检查模型。

**Workflow**：
执行预先确定步骤的流程，包括异步任务状态轮询和可安全重试的网络操作。

**执行恢复（Execution Recovery）**：
围绕尚未终止且未到截止时间的原资产请求，从可验证的恢复点、已接受的待执行提议或已知 Tripo 任务继续执行的过程。恢复保留原请求的约束、已接受答案、原操作与检查结果、澄清轮数、已消耗预算、模型调用计数、执行截止时间和执行记录。

**执行事件（Execution Event）**：
归属于特定资产请求、记录一次决策、执行或状态变化及其证据的有序记录。Agent 提议、Runtime 放行或拦截、实际工具调用和检查结果分别记录并关联。

**执行追踪（Execution Trace）**：
按顺序组织一个资产请求的执行事件及所用模型、Prompt 和 Skill 版本的记录。它为页面时间线、可展开证据、脱敏 JSON 导出及评测提供共同依据。

### Agent 评测

**案例集评测（Scenario Evaluation）**：
使用真实 Agent 和 LLM、配合固定 Tripo 测试响应，对已知场景中的意图、策略、约束和技术解释进行的非视觉行为评测。首版 20 个固定场景和连续创作阶段 8 个场景分别运行 3 次，两组独立要求关键错误为零，其余适用维度通过率不低于 90%，保留全部运行结果。

**评测案例（Evaluation Case）**：
预先给定用户输入、Tripo 测试响应、预期约束、适用检查项与允许行为的场景。Agent 提出超预算或放宽约束的动作，即使被 Runtime 拦截，也属于需要记录并判定失败的决策问题。

**关键评测错误（Critical Evaluation Error）**：
Agent 提出超预算动作、擅自放宽硬约束或伪造技术验证结论等非视觉错误。适用案例集出现任何一次，即未达到验收门槛；视觉相关断言不在当前评分范围。

**系统回归检查（System Regression Check）**：
验证并发、会话隔离、重启恢复和预算拦截等系统行为的检查，首版验收要求全部通过。其结果独立于 Agent 决策评测，Runtime 的拦截成功不能抵消 Agent 的违规提议。

**单次执行评测（Run Evaluation）**：
针对本次执行记录中可核验规则的检查结果，在演示页面与产物技术检查报告分别展示。它描述本次执行的行为，不代替案例集对不同情境下决策可靠性的评测。

### 连续创作与版本

**资产版本（Asset Version）**：
一次真实生产产物在所属会话内的不可变记录，包含文件摘要、技术报告和来源；技术未通过的候选也保留，不自动交付。

**输入版本（Input Version）**：
用户明确引用、供本次目标使用的同会话单个历史版本；它是输入，不能被登记成当前 Run 的新输出。

**输入评估（Input Assessment）**：
按当前目标重新检查输入版本所得的只读技术判断，不覆盖历史版本的原检查报告。

**几何父版本（Parent Version）**：
实际作为加工几何输入的版本；从 v1 生成 v2 后再次加工 v1 得到 v3 时，v3 的父版本仍为 v1。

**上下文引用（Context Reference）**：
仅用于解释或重新文本生成的背景引用，不等同于实际几何输入。

**正式结局（Outcome）**：
单次执行的回答、交付或结束分类。回答须零生产且无新输出；交付须引用本 Run 新产物及实测通过证据。

**公开聊天消息（Public Message）**：
从已提交用户输入、澄清、计划、操作与结果投影出的持久消息；原始 Agent 建议保留来源，不能冒充核验事实。

**退出处理中（Draining）**：
停止已持久化但原执行尚未退出的阶段，此时仍占用会话活动位置，下一目标只能保留草稿。

**技术验收上限（Acceptance Limit）**：
当前资产目标中明确接受的面数或文件体积最大值，两项独立可选。与实际测量、生产目标及系统处理容量分别记录。

**相对减面目标（Further Reduction）**：
要求输出面数实际少于指定输入的加工目标，可以不包含绝对面数上限。不能仅凭文件有效或提交的目标参数认定完成。
